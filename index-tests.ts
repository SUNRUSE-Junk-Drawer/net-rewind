// These contain types, and therefore have no logic to show up in coverage.
// Importing them from a test like this fixes coverage.

import "./asymmetric-typed";
import "./asymmetric-state";
import "./symmetric-state";
import "./state";
import "./application";

// eslint-disable-next-line @typescript-eslint/no-empty-function
it(`is yet to be implemented`, () => {});
